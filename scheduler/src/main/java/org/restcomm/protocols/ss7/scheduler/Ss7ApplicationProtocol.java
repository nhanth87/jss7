package org.restcomm.protocols.ss7.scheduler;

/** Decoded application protocol that owns a local operation code, if available. */
public enum Ss7ApplicationProtocol {

    TCAP,
    MAP,
    CAP,
    UNKNOWN
}
